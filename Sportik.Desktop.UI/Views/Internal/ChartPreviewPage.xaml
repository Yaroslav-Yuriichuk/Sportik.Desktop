<Page
    x:Class="Sportik.Desktop.UI.Views.Internal.ChartPreviewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:charting="using:WinRTXamlToolkit.Controls.DataVisualization.Charting"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="RoundedColumnDataPointStyle" TargetType="charting:ColumnDataPoint">
            <Setter Property="Background" Value="{ThemeResource SystemControlHighlightAccentBrush}" />
            <Setter Property="BorderBrush" Value="{x:Null}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="charting:ColumnDataPoint">
                        <Border x:Name="Root"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Opacity="0">
                            <Grid>
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="8,8,0,0" />
                                <TextBlock Text="{TemplateBinding FormattedDependentValue}"
                                           FontSize="24"
                                           FontWeight="Bold"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Top" />
                            </Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="RevealStates">
                                    <VisualStateGroup.Transitions>
                                        <VisualTransition GeneratedDuration="0:0:0.001" />
                                    </VisualStateGroup.Transitions>
                                    <VisualState x:Name="Shown">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1"
                                                             Duration="0" />
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Hidden">
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="Root"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0"
                                                             Duration="0" />
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Charts Preview"
                   FontSize="32"
                   FontWeight="SemiBold"
                   Margin="15 15 15 0"/>

        <Border Grid.Row="1"
                Margin="15"
                Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Padding="20">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"
                      Margin="0 0 0 18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Margin="0 0 8 0"
                            Padding="14"
                            CornerRadius="10"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <StackPanel>
                            <TextBlock Text="Total sets"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Text="{x:Bind TotalSets, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="SemiBold" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1"
                            Margin="8 0 0 0"
                            Padding="14"
                            CornerRadius="10"
                            Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
                        <StackPanel>
                            <TextBlock Text="Best day"
                                       Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" />
                            <TextBlock Text="{x:Bind BestDayLabel, Mode=OneWay}"
                                       FontSize="24"
                                       FontWeight="SemiBold" />
                        </StackPanel>
                    </Border>
                </Grid>

                <StackPanel Grid.Row="1">
                    <TextBlock Text="Temporary weekly data preview (vertical columns)."
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               Margin="0 0 0 12" />

                    <charting:Chart Height="400">

                        <charting:Chart.LegendStyle>
                            <Style TargetType="Control">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Setter Property="Width" Value="0" />
                                <Setter Property="Height" Value="0" />
                            </Style>
                        </charting:Chart.LegendStyle>

                        <charting:Chart.Axes>

                            <charting:LinearAxis Orientation="Y">
                                <charting:LinearAxis.GridLineStyle>
                                    <Style TargetType="Line">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Style>
                                </charting:LinearAxis.GridLineStyle>
                                <charting:LinearAxis.AxisLabelStyle>
                                    <Style TargetType="Control">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Setter Property="Width" Value="0" />
                                    </Style>
                                </charting:LinearAxis.AxisLabelStyle>
                            </charting:LinearAxis>

                            <charting:CategoryAxis Orientation="X">
                                <charting:CategoryAxis.GridLineStyle>
                                    <Style TargetType="Line">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Style>
                                </charting:CategoryAxis.GridLineStyle>

                                <charting:CategoryAxis.MajorTickMarkStyle>
                                    <Style TargetType="Line">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Style>
                                </charting:CategoryAxis.MajorTickMarkStyle>
                            </charting:CategoryAxis>

                        </charting:Chart.Axes>

                        <charting:ColumnSeries Title="Weekly completed sets"
                                               IndependentValuePath="Label"
                                               DependentValuePath="Count"
                                               DataPointStyle="{StaticResource RoundedColumnDataPointStyle}"
                                               ItemsSource="{x:Bind SampleData, Mode=OneWay}" />

                    </charting:Chart>

                </StackPanel>
            </Grid>

        </Border>

    </Grid>
</Page>
