<Page
    x:Class="Sportik.Desktop.UI.Views.Internal.CreateExercisePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:create="using:Sportik.Desktop.UI.ViewModels.Create"
    xmlns:converters="using:Sportik.Desktop.UI.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <create:CreateExerciseViewModel />
    </Page.DataContext>

    <Page.Resources>
        <converters:TimeSpanToLongHoursMinutesStringConverter x:Name="TimeSpanToStringConverter" />
    </Page.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Create Exercise"
                   FontSize="32"
                   FontWeight="SemiBold"
                   Margin="15 15 15 0"/>

        <Border Grid.Row="1"
                Margin="15 15 15 0"
                Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="15 15 15 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <TextBox Grid.Row="0"
                         Margin="0 0 0 15"
                         HorizontalAlignment="Left"
                         MinWidth="300"
                         PlaceholderText="Name"
                         Text="{Binding Name, Mode=TwoWay}" />

                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0 0 0 15">

                    <TextBlock Text="Repetitions:"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               Margin="0 0 15 0"
                               VerticalAlignment="Center" />

                    <ComboBox MinWidth="100"
                              ItemsSource="{Binding TargetRepetitionsOptions}"
                              SelectedValue="{Binding SelectedTargetRepetitionsOption, Mode=TwoWay}">

                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding IntValue}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>

                    </ComboBox>

                </StackPanel>

                <StackPanel Grid.Row="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0 0 0 15">

                    <TextBlock Text="Time between sets:"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               Margin="0 0 15 0"
                               VerticalAlignment="Center" />

                    <ComboBox MinWidth="100"
                              ItemsSource="{Binding TimeBetweenSetsOptions}"
                              SelectedValue="{Binding SelectedTimeBetweenSetsOption, Mode=TwoWay}">

                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TimeSpanValue, Converter={StaticResource TimeSpanToStringConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>

                    </ComboBox>

                </StackPanel>

                <StackPanel Grid.Row="3"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0 0 0 15">

                    <TextBlock Text="Execution time:"
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               Margin="0 0 15 0"
                               VerticalAlignment="Center" />

                    <ComboBox MinWidth="100"
                              ItemsSource="{Binding ExecutionTimeOptions}"
                              SelectedValue="{Binding SelectedExecutionTimeOption, Mode=TwoWay}">

                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TimeSpanValue, Converter={StaticResource TimeSpanToStringConverter}}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>

                    </ComboBox>

                </StackPanel>

            </Grid>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="15 0 15 15">

            <Button Content="Create"
                    Margin="0 0 0 0"
                    MinWidth="140"
                    Style="{ThemeResource AccentButtonStyle}"
                    Command="{Binding CreateExerciseCommand}" />

        </StackPanel>

    </Grid>
</Page>
