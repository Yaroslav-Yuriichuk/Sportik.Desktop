<Page
    x:Class="Sportik.Desktop.UI.Views.Internal.ExerciseStatisticsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:statistics="using:Sportik.Desktop.UI.ViewModels.Statistics"
    xmlns:converters="using:Sportik.Desktop.UI.Converters"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <statistics:StatisticsViewModel />
    </Page.DataContext>

    <Page.Resources>
        <converters:BoolToInvertedVisibilityConverter x:Name="BoolToInvertedVisibilityConverter" />
        <converters:DateTimeToStringConverter x:Name="DateTimeToStringConverter" />
        <converters:BoolToVisibilityConverter x:Name="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Exercise Statistics"
                   FontSize="32"
                   FontWeight="SemiBold"
                   Margin="15 15 15 0"/>

        <ListView Grid.Row="1"
                  Margin="15 15 15 0"
                  IsItemClickEnabled="False"
                  SelectionMode="None"
                  ItemsSource="{Binding WeekStatistics}">

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="statistics:WeekStatisticsViewModel">
                    <StackPanel>

                        <TextBlock FontSize="20"
                                   FontWeight="SemiBold"
                                   Margin="0 0 0 15">
                            <Run Text="{Binding FirstWeekDayDate, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='MMMM dd, yyyy'}" />
                            <Run Text="-" />
                            <Run Text="{Binding LastWeekDayDate, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter='MMMM dd, yyyy'}" />
                        </TextBlock>

                        <ListView ItemsSource="{Binding DayStatistics}"
                                  IsItemClickEnabled="True"
                                  SelectionMode="None">

                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0,0,0,12" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <Grid Background="Transparent">
                                                    <VisualStateManager.VisualStateGroups>
                                                        <VisualStateGroup x:Name="CommonStates">
                                                            <VisualState x:Name="Normal" />
                                                            <VisualState x:Name="PointerOver">
                                                                <VisualState.Setters>
                                                                    <Setter Target="CardBorder.Background" Value="{ThemeResource SystemControlHighlightListLowBrush}" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                            <VisualState x:Name="Pressed">
                                                                <VisualState.Setters>
                                                                    <Setter Target="CardBorder.Background" Value="{ThemeResource SystemControlHighlightListMediumBrush}" />
                                                                    <Setter Target="ContentPresenter.Opacity" Value="0.8" />
                                                                </VisualState.Setters>
                                                            </VisualState>
                                                        </VisualStateGroup>
                                                    </VisualStateManager.VisualStateGroups>

                                                    <Border x:Name="CardBorder"
                                                            Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                                                            BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="8" />

                                                    <ContentPresenter x:Name="ContentPresenter"
                                                                      Content="{TemplateBinding Content}"
                                                                      ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                      HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                                      Margin="{TemplateBinding Padding}" />
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="statistics:DayStatisticsViewModel">
                                    <StackPanel HorizontalAlignment="Stretch">

                                        <StackPanel Orientation="Horizontal"
                                                    HorizontalAlignment="Stretch"
                                                    Margin="15 15 15 15"
                                                    Background="Transparent">

                                            <interactivity:Interaction.Behaviors>
                                                <core:EventTriggerBehavior EventName="Tapped">
                                                    <core:InvokeCommandAction Command="{Binding ToggleCollapsedCommand}"/>
                                                </core:EventTriggerBehavior>
                                            </interactivity:Interaction.Behaviors>

                                            <SymbolIcon Symbol="Accept"
                                                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                        Width="20"
                                                        Height="20" />

                                            <TextBlock Text="{Binding Date, Converter={StaticResource DateTimeToStringConverter}, ConverterParameter=dddd}"
                                                       FontSize="16"
                                                       FontWeight="SemiBold"
                                                       Margin="15 0 0 0"/>

                                        </StackPanel>

                                        <ListView IsItemClickEnabled="False"
                                                  SelectionMode="None"
                                                  ItemsSource="{Binding ExerciseStatistics}"
                                                  Visibility="{Binding IsCollapsed, Converter={StaticResource BoolToInvertedVisibilityConverter}}">

                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="ListViewItem">
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Padding" Value="0" />
                                                </Style>
                                            </ListView.ItemContainerStyle>

                                            <ListView.ItemTemplate>
                                                <DataTemplate x:DataType="statistics:ExerciseStatisticsViewModel">
                                                    <Grid Margin="50 0 15 15">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="30"></ColumnDefinition>
                                                            <ColumnDefinition Width="2*"></ColumnDefinition>
                                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                                            <ColumnDefinition Width="*"></ColumnDefinition>
                                                        </Grid.ColumnDefinitions>

                                                        <SymbolIcon Grid.Column="0"
                                                                    Symbol="World"
                                                                    Width="20"
                                                                    Height="20"
                                                                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                                    Margin="0 0 10 0"/>

                                                        <TextBlock Grid.Column="1"
                                                                   Text="{Binding Name}"
                                                                   FontSize="14"
                                                                   FontWeight="SemiBold"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0 5 0 5" />

                                                        <TextBlock Grid.Column="2"
                                                                   FontSize="14"
                                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0 5 0 5">
                                                            <Run Text="Sets:" />
                                                            <Run Text="{Binding Sets}" FontWeight="SemiBold" />
                                                        </TextBlock>

                                                        <TextBlock Grid.Column="3"
                                                                   FontSize="14"
                                                                   Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="0 5 0 5">
                                                            <Run Text="Repetitions:" />
                                                            <Run Text="{Binding Repetitions}" FontWeight="SemiBold" />
                                                        </TextBlock>

                                                    </Grid>

                                                </DataTemplate>
                                            </ListView.ItemTemplate>

                                        </ListView>

                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>

                        </ListView>

                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="15 15 15 15">

            <Button Content="Import"
                    Margin="0 0 0 0"
                    MinWidth="140"
                    Style="{ThemeResource AccentButtonStyle}"
                    Command="{Binding ShowImportCommand}" />

        </StackPanel>

        <Grid Grid.Row="0"
              Grid.RowSpan="3"
              Background="#80000000"
              Visibility="{Binding IsImportOpen, Converter={StaticResource BoolToVisibilityConverter}}">

            <Border Width="420"
                    Background="{ThemeResource SystemControlBackgroundAltHighBrush}"
                    BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="15"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">

                <StackPanel>
                    <TextBlock Text="Import data"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Margin="0 0 0 15" />

                    <TextBlock Text="Import functionality will be available soon."
                               Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                               Margin="0 0 0 15" />

                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right">

                        <Button Content="Close"
                                MinWidth="100"
                                Command="{Binding CloseImportCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

    </Grid>
</Page>
